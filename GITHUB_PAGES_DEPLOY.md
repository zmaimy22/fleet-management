# 🚀 نشر المشروع على GitHub Pages

## خطوات النشر التلقائي (موصى به)

### 1️⃣ تفعيل GitHub Pages

1. اذهب إلى repository على GitHub:
   ```
   https://github.com/zmaimy22/fleet-management
   ```

2. اضغط على **Settings** (الإعدادات) في القائمة العلوية

3. في القائمة الجانبية، اختر **Pages**

4. في قسم **Source** (المصدر):
   - اختر: **GitHub Actions**

5. احفظ التغييرات

### 2️⃣ رفع التعديلات إلى GitHub

```bash
# إضافة جميع الملفات المعدلة
git add .

# إنشاء commit مع رسالة
git commit -m "تفعيل GitHub Pages مع النشر التلقائي"

# رفع التعديلات
git push origin main
```

### 3️⃣ انتظر النشر التلقائي

- GitHub Actions سيقوم تلقائياً ببناء ونشر المشروع
- يمكنك متابعة التقدم في تبويب **Actions** في repository
- عادة يستغرق 2-3 دقائق

### 4️⃣ زيارة الموقع

بعد اكتمال النشر، سيكون موقعك متاحاً على:

```
https://zmaimy22.github.io/fleet-management/
```

---

## 📋 معلومات إضافية

### ماذا تم إعداده؟

✅ **vite.config.js** - إضافة base URL لـ GitHub Pages
✅ **deploy.yml** - إعداد GitHub Actions للنشر التلقائي
✅ **.nojekyll** - منع GitHub من استخدام Jekyll
✅ **package.json** - إضافة script للنشر

### كيف يعمل النشر التلقائي؟

1. عند كل `git push` إلى branch `main`
2. GitHub Actions يقوم بـ:
   - ✅ تنزيل الكود
   - ✅ تثبيت dependencies
   - ✅ بناء المشروع (`npm run build`)
   - ✅ نشر المشروع على GitHub Pages

### التحقق من حالة النشر

1. اذهب إلى: `https://github.com/zmaimy22/fleet-management/actions`
2. ستجد قائمة بجميع عمليات النشر
3. ✅ علامة خضراء = نجح النشر
4. ❌ علامة حمراء = فشل النشر (اضغط عليها لمعرفة السبب)

---

## 🔧 استكشاف الأخطاء

### المشكلة: الموقع لا يعمل بعد النشر

**الحل:**
1. تأكد من تفعيل GitHub Pages في Settings → Pages
2. تأكد من اختيار "GitHub Actions" كمصدر
3. تحقق من logs في تبويب Actions

### المشكلة: الصفحات الفرعية لا تعمل

**الحل:**
هذه مشكلة معروفة مع Single Page Applications (SPA). الحل:

1. إنشاء ملف `404.html` يعيد التوجيه للصفحة الرئيسية
2. أو استخدام hash routing بدلاً من browser routing

### المشكلة: الصور أو الملفات لا تظهر

**الحل:**
تأكد من استخدام المسارات النسبية أو استخدام `base` من vite config:
```javascript
import.meta.env.BASE_URL
```

---

## 📱 النشر اليدوي (بديل)

إذا أردت النشر يدوياً بدون GitHub Actions:

### 1. تثبيت gh-pages

```bash
npm install -D gh-pages
```

### 2. تشغيل النشر

```bash
npm run deploy
```

هذا سينشئ branch جديد اسمه `gh-pages` وينشر الموقع عليه.

**ملاحظة:** عند استخدام النشر اليدوي، غيّر المصدر في Settings → Pages إلى:
- Branch: `gh-pages`
- Folder: `/ (root)`

---

## 🌐 الرابط النهائي

بعد النشر الناجح، موقعك سيكون على:

```
🔗 https://zmaimy22.github.io/fleet-management/
```

**شاركه مع فريقك!** 🎉

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. تحقق من الـ logs في GitHub Actions
2. راجع [GitHub Pages Documentation](https://docs.github.com/en/pages)
3. تأكد من صحة الإعدادات في `vite.config.js`

---

**تاريخ الإعداد:** نوفمبر 2025  
**الحالة:** ✅ جاهز للنشر
