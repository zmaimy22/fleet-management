# 🔐 نظام المصادقة والصلاحيات

## 📋 نظرة عامة

تم إضافة نظام تسجيل دخول مع مستويين من الصلاحيات:

### 👔 المسؤولون (Administradores)
- ✅ الوصول الكامل لكل الصفحات
- ✅ إدارة السائقين والمسارات
- ✅ Generate Calendar
- ✅ إدارة المستخدمين
- ✅ اعتماد/رفض طلبات الإجازات

### 🚗 السائقون (Conductores)
- ✅ رؤية جدول عملهم فقط
- ✅ طلب إجازات
- ✅ رؤية المسارات المخصصة لهم
- ❌ لا يمكنهم التعديل على أي شيء

---

## 🚀 المكونات الجديدة

### 1. `AuthContext.jsx`
Context للمصادقة وإدارة الجلسة

### 2. `Login.jsx`
صفحة تسجيل الدخول

### 3. `DriverDashboard.jsx`
لوحة السائق - تعرض:
- جدول العمل الشخصي
- نموذج طلب إجازة
- مسارات العمل

### 4. `UserManagement.jsx`
إدارة المستخدمين (للمسؤولين فقط)

---

## 🔑 المستخدمون الافتراضيون

### مسؤول:
- Username: `admin`
- Password: `admin123`
- الوصول: كامل

### سائق تجريبي:
- Username: `conductor`
- Password: `1234`
- مرتبط بـ: أول سائق في القائمة

---

## 🛠️ التكامل مع App.jsx

سيتم تحديث `App.jsx` ليتضمن:

```javascript
import { AuthProvider, useAuth } from './contexts/AuthContext';
import Login from './components/Login';
import DriverDashboard from './components/DriverDashboard';
```

### منطق العرض:
```javascript
if (!user) {
  return <Login />;
}

if (user.role === 'driver') {
  return <DriverDashboard />;
}

// Admin sees full system
return <AdminView />;
```

---

## 📝 إضافة مستخدم جديد

### للمسؤولين:
1. افتح `Navbar → Usuarios`
2. اضغط "Nuevo Usuario"
3. أدخل البيانات:
   - Usuario (username)
   - Contraseña
   - Nombre Completo
   - Rol (Admin / Conductor)
   - إذا كان conductor: اختر السائق من القائمة
4. احفظ

### برمجياً:
```javascript
const newUser = {
  id: Date.now(),
  username: 'juan',
  password: '1234',
  name: 'Juan Pérez',
  role: 'driver',
  driverId: 5 // ID من قائمة drivers
};
```

---

## 🔒 الأمان

### ملاحظات مهمة:
⚠️ **هذا نظام بسيط للتطوير**

للإنتاج، يُنصح بـ:
1. تشفير كلمات المرور (bcrypt)
2. JWT tokens بدلاً من localStorage
3. Backend API للمصادقة
4. HTTPS إلزامي
5. Rate limiting
6. Session timeout

---

## 📊 تخزين البيانات

### localStorage:
- `users` - قائمة المستخدمين
- `currentUser` - المستخدم الحالي (session)

---

## 🎨 الصفحات

### صفحة تسجيل الدخول
- تصميم أنيق
- معلومات المستخدمين التجريبيين
- رسائل خطأ واضحة

### لوحة السائق
- عرض التقويم الشخصي
- نموذج طلب إجازة بسيط
- ألوان واضحة للحالات

### إدارة المستخدمين
- جدول بجميع المستخدمين
- إضافة/تعديل/حذف
- ربط السائقين بحساباتهم

---

## 🔄 سير العمل

### للسائق:
```
1. Login → DriverDashboard
2. رؤية الجدول
3. طلب إجازة
4. Logout
```

### للمسؤول:
```
1. Login → Full System
2. إدارة كل شيء
3. اعتماد الإجازات
4. إدارة المستخدمين
5. Logout
```

---

## 📝 المهام القادمة

- [ ] تكامل مع Backend API
- [ ] تشفير كلمات المرور
- [ ] JWT authentication
- [ ] إشعارات للسائقين
- [ ] تاريخ طلبات الإجازات
- [ ] تقارير للمسؤولين

---

**النظام الأساسي جاهز! 🎉**
