# 🔗 المسارات المترابطة / Rutas Relacionadas

## المفهوم الأساسي

في نظام إدارة الأسطول، بعض المسارات يقوم بها **نفس السائق** في نفس اليوم.

### مثال:
- **R1** (مسار رئيسي) + **R1.1** (مسار ثانوي) = يقوم بهما نفس السائق
- **R2** (مسار رئيسي) + **R2.2** (مسار ثانوي) = يقوم بهما نفس السائق
- **R3** (مسار رئيسي) + **R3.1** (مسار ثانوي) = يقوم بهما نفس السائق

## البنية في النظام

```javascript
{
  code: 'R1 2391MKG',
  shortCode: 'R1',
  isSecondary: false  // مسار رئيسي
}

{
  code: 'R1.1 6173HDP',
  shortCode: 'R1.1',
  isSecondary: true,  // مسار ثانوي
  mainRoute: 'R1'     // مرتبط بالمسار R1
}
```

## الدوال المتاحة

### 1. `getRelatedRoutes(routeCode)`
تعيد جميع المسارات المرتبطة بمسار معين

```javascript
const { getRelatedRoutes } = useRoutes();

// إذا أعطيتها R1، ستعيد: [R1, R1.1]
const routes = getRelatedRoutes('R1');
// النتيجة: [{code: 'R1', ...}, {code: 'R1.1', ...}]

// إذا أعطيتها R1.1، ستعيد أيضاً: [R1, R1.1]
const routes = getRelatedRoutes('R1.1');
// النتيجة: [{code: 'R1', ...}, {code: 'R1.1', ...}]
```

### 2. `getRouteGroupLabel(routeCode)`
تعيد نص يوضح المسارات المجمعة

```javascript
const { getRouteGroupLabel } = useRoutes();

const label = getRouteGroupLabel('R1');
// النتيجة: "R1+R1.1"

const label = getRouteGroupLabel('R3');
// النتيجة: "R3+R3.1"
```

### 3. `areRoutesRelated(route1, route2)`
تتحقق إذا كان مساران مرتبطان

```javascript
const { areRoutesRelated } = useRoutes();

const related = areRoutesRelated('R1', 'R1.1');
// النتيجة: true

const related = areRoutesRelated('R1', 'R2');
// النتيجة: false
```

### 4. `getMainRouteCode(routeCode)`
تعيد المسار الرئيسي لأي مسار

```javascript
const { getMainRouteCode } = useRoutes();

const main = getMainRouteCode('R1.1');
// النتيجة: "R1"

const main = getMainRouteCode('R1');
// النتيجة: "R1"
```

## المسارات المترابطة الحالية

| المسار الرئيسي | المسارات الثانوية | السائق |
|----------------|-------------------|--------|
| R1             | R1.1              | نفس السائق |
| R2             | R2.2              | نفس السائق |
| R3             | R3.1              | نفس السائق |
| R7             | R7.1              | نفس السائق |

## استخدام في التقويم

عند عرض جدول السائقين:
- إذا كان السائق يقود **R1**، سيظهر: **R1+R1.1**
- إذا كان السائق يقود **R2**، سيظهر: **R2+R2.2**

## ملاحظات مهمة

1. ✅ المسار الثانوي **دائماً** يُنفذ بعد المسار الرئيسي
2. ✅ نفس السائق يقوم بالمسارين في نفس اليوم
3. ✅ عند تعيين مسار رئيسي لسائق، يُعتبر مسؤولاً تلقائياً عن المسار الثانوي
4. ⚠️ لا يمكن تعيين المسار الثانوي لسائق مختلف عن المسار الرئيسي

## مثال تطبيقي

```javascript
// في مكون التقويم
import { useRoutes } from './hooks/useRoutes';

function Calendar() {
  const { getRouteGroupLabel, areRoutesRelated } = useRoutes();
  
  // عرض المسار مع المسارات المرتبطة
  const displayRoute = (routeCode) => {
    return getRouteGroupLabel(routeCode); // "R1+R1.1"
  };
  
  // التحقق قبل تعيين مسار لسائق
  const canAssignRoute = (driver, newRoute) => {
    const currentRoutes = driver.assignedRoutes || [];
    
    // تحقق إذا كان السائق يقود مسار مرتبط بالفعل
    for (const route of currentRoutes) {
      if (areRoutesRelated(route, newRoute)) {
        return true; // يمكن التعيين - مسارات مرتبطة
      }
    }
    
    return false;
  };
  
  return (
    // JSX code...
  );
}
```

## التحديثات المستقبلية

- [ ] إضافة تحذير عند محاولة تعيين مسار ثانوي لسائق مختلف
- [ ] عرض تلقائي للمسارات المرتبطة في واجهة تعيين المسارات
- [ ] إحصائيات خاصة بالمسارات المجمعة

---

**تاريخ التحديث:** نوفمبر 2025  
**الإصدار:** 1.0.0
